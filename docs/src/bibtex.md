The BibTeX parser is the core parser shipped with `BibParser.jl`.

It accepts both plain BibTeX and BibLaTeX syntax, and it can return either the
legacy entry dictionary or a lossless document model.

```@contents
```

```@autodocs
Modules = [BibParser.BibTeX]
Pages   = ["bibtex.jl"]
```
